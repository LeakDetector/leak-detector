How to install the leak detector backend
========================================
1. Install `bro`, the network analyzer that Leak Detector uses.  To do so, run "install-bro.py".

2. Run "python setup.py install".
